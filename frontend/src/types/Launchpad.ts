export type Launchpad = {
  id: string;
  details: string;
  full_name: string;
  images: {
    large: string[];
  }
  locality: string;
  name: string;
  region: string;
  status: string;
  timezone: string;
}